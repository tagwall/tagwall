// Injected at build time by Vite `define` (see vite.config.ts).
declare const __BUILD_TIME__: string // ISO 8601 build timestamp
declare const __BUILD_COMMIT__: string // short git commit, or 'local'
